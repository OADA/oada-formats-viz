import React from 'react'
import {connect} from 'cerebral-view-react'

export default connect({
  // State connections:

}, {
  // Signal connections:
},

  // Core class
  function App (props) {

    return (
      <div>

        Hello.

      </div>
    );

  }
)
